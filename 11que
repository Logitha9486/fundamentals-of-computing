//Summing up 11+22+33+44+ …. n
int Series(int n)
{
	int i;
	int sums = 0;
	for (i = 1; i <= n; i++)
		sums += (i * i);
	return sums;
}

int main()
{
	int n ;
	printf("enter a value");
	scanf("%d",&n);
	int res = Series(n);
	printf("%d", res);
}
